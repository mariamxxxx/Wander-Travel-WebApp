<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 20px;
        }
        
        .navbar {
            background: rgba(0, 0, 0, 0.8) !important;
            backdrop-filter: blur(10px);
            border-bottom: 2px solid #00ff88;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }
        
        .navbar-brand {
            color: #00ff88 !important;
            font-weight: bold;
            font-size: 1.5rem;
            text-shadow: 0 0 10px #00ff88;
        }
        
        .welcome-container {
            text-align: center;
            margin: 40px 0;
            padding: 30px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }
        
        .welcome-title {
            color: white;
            font-size: 3rem;
            font-weight: bold;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
            margin-bottom: 10px;
        }
        
        .welcome-subtitle {
            color: #ccffcc;
            font-size: 1.2rem;
            opacity: 0.9;
        }
        
        .categories-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 30px;
            margin: 40px auto;
            max-width: 1200px;
        }
        
        .category-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 25px;
            width: 300px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        .category-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
            border-color: #00ff88;
        }
        
        .category-image {
            width: 150px;
            height: 150px;
            object-fit: cover;
            border-radius: 50%;
            border: 3px solid #00ff88;
            padding: 5px;
            background: white;
            margin-bottom: 20px;
            box-shadow: 0 0 20px rgba(0, 255, 136, 0.5);
        }
        
        .category-name {
            color: white;
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 15px;
            text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.5);
        }
        
        .view-btn {
            background: linear-gradient(45deg, #00ff88, #00cc6a);
            border: none;
            color: black;
            font-weight: bold;
            padding: 12px 30px;
            border-radius: 30px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
            box-shadow: 0 5px 15px rgba(0, 255, 136, 0.3);
        }
        
        .view-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(0, 255, 136, 0.5);
        }
        
        .wanttogo-container {
            text-align: center;
            margin: 50px 0;
        }
        
        .wanttogo-btn {
            background: linear-gradient(45deg, #ff6b6b, #ff8e53);
            border: none;
            color: white;
            font-weight: bold;
            padding: 15px 50px;
            border-radius: 30px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 20px rgba(255, 107, 107, 0.4);
        }
        
        .wanttogo-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 25px rgba(255, 107, 107, 0.6);
        }
        
        .search-form {
            background: rgba(255, 255, 255, 0.1);
            padding: 10px;
            border-radius: 30px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .search-input {
            background: transparent;
            border: none;
            color: white;
            padding: 8px 20px;
        }
        
        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }
        
        .search-input:focus {
            outline: none;
            box-shadow: none;
        }
        
        .search-btn {
            background: #00ff88;
            border: none;
            color: black;
            border-radius: 20px;
            padding: 8px 25px;
            font-weight: bold;
        }
        
        .footer {
            text-align: center;
            color: rgba(255, 255, 255, 0.6);
            margin-top: 50px;
            padding: 20px;
            font-size: 0.9rem;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Destinations | Home</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/home">
                <i class="fas fa-globe-americas"></i> Travel Explorer
            </a>
            
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <form method="POST" action='/search' class="search-form form-inline">
                            <input class="form-control search-input mr-sm-2" name="Search" type="search" 
                                   placeholder="Search destinations..." aria-label="Search">
                            <button class="btn search-btn my-2 my-sm-0" type="submit">
                                <i class="fas fa-search"></i> Search
                            </button>
                        </form>
                    </li>
                    <li class="nav-item ml-3">
                        <form action="/logout" method="POST">
                            <button type="submit" class="btn btn-outline-light">
                                <i class="fas fa-sign-out-alt"></i> Logout
                            </button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Welcome Section -->
    <div class="container">
        <div class="welcome-container">
            <h1 class="welcome-title">
                <i class="fas fa-compass"></i> Explore the World
            </h1>
            <p class="welcome-subtitle">
                Discover amazing destinations for your next adventure
            </p>
        </div>

        <!-- Categories Section -->
        <div class="categories-container">
            <!-- Hiking Card -->
            <div class="category-card">
                <img src="/hiking.png" alt="Hiking" class="category-image">
                <h3 class="category-name">Mountain Adventures</h3>
                <p class="text-light mb-4">Explore breathtaking trails and peaks</p>
                <button onclick="location.href = '/hiking';" class="view-btn">
                    <i class="fas fa-mountain"></i> Explore Hiking
                </button>
            </div>

            <!-- Cities Card -->
            <div class="category-card">
                <img src="/cities.png" alt="Cities" class="category-image">
                <h3 class="category-name">Urban Cities</h3>
                <p class="text-light mb-4">Experience vibrant city life and culture</p>
                <button onclick="location.href = '/cities';" class="view-btn">
                    <i class="fas fa-city"></i> Explore Cities
                </button>
            </div>

            <!-- Islands Card -->
            <div class="category-card">
                <img src="/islands.png" alt="Islands" class="category-image">
                <h3 class="category-name">Tropical Islands</h3>
                <p class="text-light mb-4">Relax on beautiful beaches and islands</p>
                <button onclick="location.href = '/islands';" class="view-btn">
                    <i class="fas fa-umbrella-beach"></i> Explore Islands
                </button>
            </div>
        </div>

        <!-- Want-to-Go List Button -->
        <div class="wanttogo-container">
            <button onclick="location.href = '/wanttogo';" class="wanttogo-btn">
                <i class="fas fa-heart"></i> View My Want-to-Go List
            </button>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>© 2025 Travel Explorer | Find your next adventure</p>
            <p class="mt-2">
                <i class="fas fa-users text-warning"></i> 
                Currently exploring: 1,247 travelers online
            </p>
        </div>
    </div>

    <!-- Add logout route in auth.js for the logout button to work -->
    <script>
        // Simple hover effects
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.category-card');
            cards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-10px)';
                });
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
        });
    </script>
</body>
</html>